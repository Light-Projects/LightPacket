{
  "Proto": {
    "Name": "EAP_PEAP",
    "Description": "Protected Extensible Authentication Protocol (RFC 7486). PEAP creates a TLS tunnel between the client and authentication server, then runs another EAP method inside the tunnel. Widely used in WPA2/WPA3-Enterprise networks.",
    "Fields": [
      "code             {8 bits}    = 1",
      "id               {8 bits}    = 1",
      "length           {16 bits}   = auto-calculated",
      "type             {8 bits}    = 25",
      "L                {1 bit}     = 0",
      "M                {1 bit}     = 0",
      "S                {1 bit}     = 0",
      "reserved         {2 bits}    = 0",
      "version          {3 bits}    = 0",
      "tls_message_len  {32 bits}   = 0 (only present if L = 1)",
      "tls_data         {variable}  = b''"
    ]
  }
}